#!/usr/bin/env bash
# Wrapper script for passgen.py
# Allows running: passgen generate, passgen list, passgen get, etc.

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
python3 "$SCRIPT_DIR/passgen.py" "$@"
